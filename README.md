# Portfolio

Minimal single-page portfolio built with Next.js, React, TypeScript, and Tailwind CSS.

## Vercel config
1. In Vercel Project Settings, set **Framework Preset** to **Next.js**.
2. Leave **Output Directory** empty (do not set `public`, `build`, or any custom folder).
3. Build command can remain the default or **npm run build**.
